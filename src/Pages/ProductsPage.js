import axiosClient from "./Products/Posts";
import { useEffect } from "react";
function ProductsPage () {
    useEffect (() => {
        const fetch = async () => {
            try {
                const res = await axiosClient.get
            }
        }
    })

    return (

    )
}